export type LandingFeature = {
  title: string;
  description: string;
};

export type ProductSpec = {
  label: string;
  value: string;
};

export type ProductDetail = {
  title: string;
  description: string;
};

export type ProductImage = {
  src: string;
  alt: string;
  title: string;
  description: string;
  width: number;
  height: number;
};
